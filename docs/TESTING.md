# 功能测试清单（NeverBroke）

上线前建议在真机与开发者工具各跑一遍。

> 说明：以下状态基于当前仓库代码实现与文档核对更新，仅将“代码已覆盖/逻辑已实现”的项目标记为已具备测试条件或已完成修复；并不等同于已完成完整人工回归。涉及微信授权、订阅消息、文件选择、Canvas 渲染的场景，仍需补充实测。

## 认证与引导

- [x] 未配置 `CLOUD_ENV_ID` 时登录有明确提示（`app.js` 启动日志 + 登录页弹窗已实现）
- [x] 勾选协议后可登录；拒绝头像昵称仍可登录（登录页已处理拒绝 `getUserProfile` 的兜底分支）
- [x] 新用户弹窗「去创建账户」与「稍后」分支正常（新用户欢迎弹窗与跳转分支已实现）
- [x] 云函数返回 401 时自动清本地用户并回登录页（`utils/request.js` 已统一处理）

## 首页 / 账本

- [x] 下拉刷新首页数据（相关页面已接入下拉刷新/重新加载逻辑；首页联动仍建议补充手测）
- [x] 无账户时提示条与跳转新建账户（首页文案与创建账户入口已具备）
- [x] 未设预算时展示「去设置」（首页文案与设置入口已具备）
- [x] 记一笔：常用标签、编辑账单、删除与确认（常用模板、编辑/删除流程均已实现）
- [x] 账本：日期、类型/账户/分类筛选；转账仅删除无编辑（筛选与转账不可编辑限制已实现）

## 账户 / 预算 / 分期

- [ ] 新建/编辑账户；信用卡额度；归档
- [x] 预算保存、历史列表、从上月复制、80%/超支弹窗（同月仅一次）（预算页面与本地提醒去重逻辑已实现）
- [x] 分期预览、列表、明细、标记已还（页面逻辑已完整接通）

## 报表 / 时间线 / 现金流

- [x] 报表月份切换；Canvas 饼图与折线图渲染（月份切换与 Canvas 绘制代码已实现，需重点补真机/开发者工具渲染验证）
- [x] 时间线历史 30/60/90；未来合并周期收入与分期（历史区间切换、未来收入/分期聚合已实现）
- [x] 现金流 30/60/90；安全线在设置中配置后风险提示（设置读取安全线并传入预测逻辑已实现）

## 周期收入 / 薪资 / 导入导出

- [x] 周期收入到期标识、实现入账、删除（到期标识、入账、删除逻辑已实现）
- [x] 薪资计算与方案保存/删除（计算、保存方案、删除方案已实现）
- [ ] CSV 导入预览与执行；导出快捷范围与下载/打开（导入预览/执行已实现；导出能力本轮未确认，需保留待测试）

## 设置

- [x] 语言切换后 Tab 文案变化；隐藏金额全局生效（TabBar 动态国际化已实现；隐藏金额需补跨页联动实测）
- [ ] 订阅消息：配置 `subscribe.js` 模板 ID 后可发起授权，服务端提醒还需部署 `notify` 云函数、定时触发器与 `notification_log` 集合

## 多语言（i18n）

- [x] 设置中切换 **中文 / English** 后保存：底部 Tab、当前页导航标题与主要文案应更新（`applyTabBarI18n` 与各页 `loadI18n` 已接入）
- [x] 登录页：Logo 下展示 `LOGO.md` 副标题文案；协议与一键登录按钮随语言切换（需重新进入登录页验证）（登录页文案映射已实现）
- [x] 账本明细、记一笔、转账、报表/现金流/时间线、预算/分期/周期收入/薪资、导入、个人中心、设置 无大块残留中文（分类名等数据字段可仍为中文）（本轮变更覆盖到主要页面）

## 当前仍需重点手测的未闭环项

- [ ] 账户管理全流程（新建 / 编辑 / 信用卡额度 / 归档）
- [ ] CSV 导出能力（快捷范围、下载、打开）
- [ ] 隐藏金额是否在首页、账本、报表等所有页面真正全局生效
- [ ] 订阅消息授权链路（需模板 ID + 真机）
- [ ] Canvas 图表在不同机型/基础库下的渲染稳定性
