<picker mode="selector" range="{{range}}" value="{{index}}" bindchange="onChange">
  <view class="nb-picker">
    <text class="nb-picker__text">{{display}}</text>
    <text class="nb-picker__arrow">▼</text>
  </view>
</picker>
